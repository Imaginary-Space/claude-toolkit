# Commit style

- Use imperative mood in the subject (`add`, `fix`, not `added`, `fixed`).
- Keep the subject ~72 characters or less when practical.
- Optional scope in parentheses: `fix(auth): handle null refresh token`.
- Put rationale and tradeoffs in the body when non-obvious.
